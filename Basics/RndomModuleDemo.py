import random
"""
Random Module :
---------------
1.random --module generates the  random numbers from the 
1. seed(2) ---ge erates same numbers for multiple runs --by using random() method.

2.choice(self, seq)
     |      Choose a random element from a non-empty sequence.
3.randint(self, a, b)
     |      Return random integer in range [a, b], including both end points.
4.randrange(self, start, stop=None, step=1, _int=<class 'int'>)
     |      Choose a random item from range(start, stop[, step]).
      
5.sample(self, population, k)
     |      Chooses k unique random elements from a population sequence or set.

"""
help(random)